# Senha do banco de dados PostgreSQL
# Troque por uma senha forte antes de rodar em produção!
DB_PASSWORD=troque_por_senha_forte_aqui

# Segredo JWT para autenticação (mínimo 32 caracteres)
# Gere com: openssl rand -hex 32
JWT_SECRET=troque_por_segredo_aleatorio_forte_aqui_min32chars
